# 个人博客项目总结

> 一个部署在双平台的 Hexo 个人博客：写作即发布，侧边栏即全站导航，国内免梯可达。
>
> - 线上地址（国内直连推荐）：https://lgq-828.pages.dev
> - 线上地址（备用）：https://jiang058280.github.io
> - 仓库：https://github.com/jiang058280/jiang058280.github.io

---

## 一、项目最终实现了什么

### 1. 博客本体
- 基于 Hexo + Butterfly 主题的静态博客，中文配置齐全
- 首页、文章页、归档、标签、分类、公告、个人简介 七大页面
- 深色模式自动适配、移动端抽屉菜单、响应式布局

### 2. 定制侧边栏（项目最大特色）
- **全站导航面板**：头像名片 + 页面入口 + 分类体系 + 每篇文章直达
- **三级分组树**：文章 → 分类（自动按文章类型建组）→ 文章标题
  - 新文章标注分类后 push，会自动出现在对应分类子标签下，零维护
  - 到达任何页面时，对应入口自动展开并高亮
- **液态玻璃视觉**：独立玻璃胶囊菜单、毛玻璃、内高光、柔影，深色模式同步
- 纯白背景、点击谁高亮谁、无文字光标/选区等交互细节全部打磨

### 3. 科幻风个人简介页
- 全宽布局（自动隐藏右侧栏，内容占满），网格坐标底纹
- HUD 档案抬头（四角括号面板 + 呼吸绿灯在线状态 + 数据读出）
- 技术栈模块化（MODULE 01/02/03 面板 + 等宽字体芯片）
- 项目经历「任务日志」式呈现（项目背景/主要工作/成果高亮条）
- 自我评价面板与全站格式统一；**全中文文案**，访客零理解成本

### 4. 双平台自动部署
- push 一次代码，同时发布到 GitHub Pages 和 Cloudflare Pages
- 全程零密钥、零手动操作

---

## 二、技术栈明细

| 层 | 技术 | 用途 |
|---|---|---|
| 静态站点生成 | Hexo 7.3 | Markdown → 静态 HTML |
| 主题 | Butterfly 5.7 | 基础模板，经配置覆盖 + CSS 注入深度定制 |
| 语言 | 原生 JavaScript / CSS | 导航坞逻辑、液态玻璃样式（无框架依赖） |
| 运行环境 | Node.js 20 | 本地与 CI 构建一致 |
| 部署 ① | GitHub Actions + `actions/deploy-pages` | OIDC 零密钥发布到 GitHub Pages |
| 部署 ② | Cloudflare Pages（Git 集成） | 自动构建发布，`pages.dev` 国内直连可用 |
| 托管域名 | `jiang058280.github.io` / `lgq-828.pages.dev` | 双平台互为备份 |
| 图标 | Font Awesome 7（自托管） | 导航图标 |

---

## 三、架构与发布流程

```
写文章 (source/_posts/*.md)
        │
        ▼ git push
GitHub 仓库 (jiang058280.github.io)
        │
        ├──► GitHub Actions ──► GitHub Pages（jiang058280.github.io）
        │
        └──► Cloudflare Pages ──► lgq-828.pages.dev（国内直连）

每次构建自动完成：
  1. npm install 安装锁定版本的依赖
  2. npx hexo generate 生成静态页面
  3. scripts/content_index.js 生成全站内容索引（侧边栏文章列表的数据源）
```

---

## 四、已经做对、但容易忽略的工程细节

这些是做的过程中顺手解决/规避的问题，属于"当时没觉得，回头看很关键"的部分：

### 1. 零外部 CDN，资源 100% 自托管
Font Awesome、pjax、instant.page、infinitegrid 全部下载进项目内。
→ 不受 jsdelivr 等国外 CDN 波动影响，国内加载稳定；这也是页面切换快的根基。

### 2. workers.dev 被墙的提前规避
首次 Cloudflare 部署用的是 Workers（`workers.dev` 域名被 DNS 污染，实测直连失败），
随即改用 Pages（`pages.dev`）——实测国内直连 200。
→ 免梯访问的目标真正达成。

### 3. pjax 局部刷新 + instant.page 悬停预取
点击菜单只替换内容区不整页重载；鼠标悬停即预加载目标页。
→ 切换页面基本秒开。

### 4. 构建时内容索引（侧边栏零维护的关键）
`scripts/content_index.js` 在每次构建时自动扫描全站文章生成 `content-index.json`，
侧边栏读取后动态填充文章列表。
→ 以后写 100 篇文章，侧边栏自动跟上，不需要手动改任何菜单配置。

### 5. 静态资源版本号防缓存
所有自定义 CSS/JS 引用带 `?v=日期` 参数，改动样式同步升版本号。
→ 避免了"改了代码但访客浏览器还显示旧样式"这类最难排查的问题。

### 6. 侧边栏克隆到 body 根节点，绕开主题 transform 陷阱
Butterfly 滚动时会 transform 顶栏，导致 fixed 子元素跟着跑。
导航坞改为 JS 克隆到页面根节点，彻底脱离影响。

### 7. 零密钥部署
GitHub Actions 使用官方 OIDC 令牌发布 Pages，仓库里没有任何密钥/令牌需要保管。

### 8. 隐私细节
简历页曾包含手机号/邮箱，按需求移除——公网页面不留可直接骚扰的联系方式。

### 9. 无障碍与体验细节
- 侧边栏 `user-select: none`：点击菜单不会出现文字光标/误选文字
- `prefers-reduced-motion` 适配：系统开启"减少动态效果"时关闭动画
- 高亮逻辑：点击谁高亮谁；同址多入口只亮一个；自动展开定位当前页

### 10. 项目完全自包含、可复现、可回滚
- 所有依赖/缓存/工具都在 `D:\boke` 项目文件夹内，没污染系统全局环境
- 换一台电脑：装 Node.js → `npm install` → `npx hexo server` 即可完整复现
- 每个功能一个 git 提交，任何改动可随时回滚

---

## 五、目录结构

```
D:\boke
├── _config.yml                # 站点配置（标题/语言/URL/主题）
├── _config.butterfly.yml      # 主题覆盖配置（菜单/注入/pjax/CDN 自托管）
├── package.json               # 依赖锁定（CI 与本地一致）
├── scripts/
│   └── content_index.js       # 构建时生成全站内容索引
├── source/
│   ├── _posts/                # ★ 所有文章（写在这里）
│   ├── about/                 # 个人简介页
│   ├── announcement/          # 公告页
│   ├── categories/ tags/      # 分类/标签聚合页
│   ├── css/custom.css         # 全站自定义样式（液态玻璃/科幻风）
│   ├── js/custom.js           # 导航坞逻辑（克隆/高亮/内容索引）
│   └── img/                   # 头像等本地图片
├── .github/workflows/
│   └── deploy.yml             # GitHub Actions 自动部署
└── README.md
```

---

## 六、常用操作速查

| 想做什么 | 怎么做 |
|---|---|
| 写新文章 | `npm run new -- "标题"`，编辑 `source/_posts/标题.md` |
| 本地预览 | `npm run server` → http://localhost:4000 |
| 发布 | `git add . && git commit -m "..." && git push`（1-3 分钟双端自动上线） |
| 调侧边栏菜单 | 改 `_config.butterfly.yml` 的 `menu:` 段 |
| 调样式 | 改 `source/css/custom.css`，**记得把 inject 里的 `?v=` 版本号 +1** |
| 发公告 | 编辑 `source/announcement/index.md` |

---

## 七、已知限制与可扩展方向

- **已知限制**
  - 纯白侧边栏上玻璃质感偏含蓄（白底上透明度差异小），如需更强玻璃感可加淡色底纹
  - 文章多了以后侧边栏文章列表会变长，届时可加"只显示最近 10 篇"
  - `pages.dev` 国内可达性总体良好但非 100% 保证，极端情况可绑定自有域名
- **可扩展方向（都留好了接口）**
  - 评论系统（giscus/Waline）、站点统计、自定义域名（Cloudflare 一键绑定）
  - 新分类自动出现在侧边栏分组树，无需配置
